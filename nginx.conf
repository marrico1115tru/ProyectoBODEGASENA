server {
  listen 80;

  location / {
    root   /usr/share/nginx/html;
    index  index.html index.htm;
    # Esta línea es clave para que las rutas de React (SPA) funcionen
    try_files $uri $uri/ /index.html;
  }
}